# 매매테스트 UI 통일 및 전략 확인

## 변경 범위

- 시장 리포트의 상단 메뉴, 흰색/파란색 기본 스타일, 날짜별 사이드바와 페이지 배치를 재사용했다.
- 매매 보고서의 큰 장식형 제목/배경을 줄이고 계좌 비교, 체결 표, 다음 행동을 중심으로 정리했다.
- 보고일/미국장/매입일 표시는 시장 리포트와 같은 `YYYY.MM.DD`로 통일했다.
- 생성 시각은 `YYYY.MM.DD HH:mm KST`로 표시하고, 브라우저 시간대와 무관하게 한국 시각을 사용한다.
- API 요청과 저장된 날짜 키는 원래 `YYYY-MM-DD` 그대로다. 한국 보고일/미국 거래일 계산, 휴장일 판단, 예약, 원장 기준은 변경하지 않았다.
- 다른 날짜를 조회하는 동안 이전 날짜의 본문을 보여주지 않는다. 조회 실패/빈 기록 상태를 명시한다.
- 수행 전략, 전략 설정, 가격 수집, 기록 및 체결 자료는 수정하지 않았다. 매매 workflow 재실행/원장 초기화도 하지 않았다.

## 전략 확인 결과

| 요청한 이름 | 확인 결과 |
| --- | --- |
| I1 | 현재 코드/설정/문서, 로컬 데이터 및 접근 가능한 로컬 Git 이력에서 이 이름에 해당하는 설명이나 구현을 찾지 못했다. G55와 동일한 방법이라고 가정할 근거가 없다. |
| R2 | `IRCS-BBCCI-M-R2`로 설명과 구현이 남아 있으며 현재 운영 중이다. Cloudflare R2 저장소를 의미하는 것이 아니라 전략 이름이다. |

현재 운영 전략은 `IRCS-BBCCI-M-G55`와 `IRCS-BBCCI-M-R2`다.
2026-09-11 읽기 전용 공개 API 확인: 최신 한국 보고일 9월 10일, 미국 거래일 9월 9일,
전략 버전 `IRCS-G55-R2-FORWARD-2026-09-01`. G55 1종목, R2 4종목의 보유 기록을 확인했다.

### R2에 남아 있는 조건

- 공통 IRCS 진입: 최근 10거래일 내 종가의 볼린저 하단 접촉, 최근 3거래일 내 CCI -100 상향 통과 및 CCI 신호선 상향 교차, 현재 CCI가 신호선 위이고 종가가 볼린저 중단 아래.
- 공통 시장 조건: IVV CCI -100~-50, `(종가-볼린저 하단)/(볼린저 중단-하단)` 0.40~0.80.
- R2 추가 조건: IVV CCI가 전 거래일보다 상승하고, 종목의 볼린저 중단까지 목표 여유가 2% 이상.
- G55의 CCI-신호선 차이 55.003489 조건은 별도 전략 조건이며 R2 조건으로 합치지 않는다.
- 계좌별 최초 자금 $21,000, 최대 4종목, 동일 비중 및 테마 중복 제한.
- 신호일에 결정한 행동은 다음 완료 미국 거래일의 조정종가와 기존 비용 모형으로 처리한다.
- 볼린저 중단 도달 또는 S&P 500 구성종목 제외 시 매도 조건을 적용한다.

근거: `config/ircs-forward.yaml`, `market_report_pipeline/us_ircs_forward_report.py`의
`_signals`, `_market_gate`, `_passes_entry_variant`, `make_decision`, 원장 저장 코드.
UI의 검증 원칙에도 R2 추가 조건이 표시되어 있다.

## 기존 기록을 이용한 향후 검증에 대한 구분

보존된 G55/R2 보고서와 원장 구조에는 신호, 체결, 보유, 비용 및 후보 스냅샷 기록이 있다.
이 기록은 해당 전략으로 산출된 가상 수행 기록이지 I1 수행 기록이 아니다.
I1을 과거부터 수행했다고 가정하려면 먼저 I1의 정확한 진입/청산/배분 규칙을 확인해야 한다.
그 이후의 재현 결과는 기존 기록을 덮어쓰지 않는 별도 가상 검증으로 구분해야 한다.
이번 작업에서는 전략 전환, 과거 전략 재계산, 새로운 전략 검증을 실행하지 않았다.

## 검증

- 화면/Worker 121개 테스트 통과. UTC/한국 환경/미국 환경 실행에서 동일하게 통과.
- Python 기존 86개 테스트 통과. 프로덕션 빌드 통과.
- Playwright: 1440/390/320px x UTC/Asia/Seoul/America/New_York 9개 조합 통과.
- 실제 공개 보고서 자료 및 이전 M/R2 기록으로 날짜 동일성, 기록 전환, 겹침/전체 페이지 넘침 없음, 브라우저 예외 없음 확인.
- 추가 UI 테스트: 날짜·시각 표시, 원본 객체/요청 날짜/전략 보존, 이전 날짜 잔상 방지, 조회 실패, 이전 전략 표시, 빈 목록.
- 화면 증거: `artifacts/trading-ui-2026-09-11/trading-1440.png`, `trading-390.png`, `trading-320.png`.
- 로컬 확인 주소: http://127.0.0.1:5175/trading-test-report
