# 가격 원본 및 준비 시각 진단

## 목적과 기존 문제

9월 11일 오전 실행은 미국 9월 10일 가격을 올바르게 요청했지만 547개 종가가 결측이었다.
오후 동일 코드 실행에서 전체 553개가 정상 수집됐다. 과거 원시 응답은 소급 복원할 수 없다.
이번 변경은 다음 발생 때 외부 응답 결측과 파서 실패를 구분하고, 실행 시각을 결정할 관측 근거를 누적한다.

## 자동 실행과 확인 위치

- Actions 이름: `Diagnose price availability and timing`
- 실행 파일: `.github/workflows/diagnose-price-availability.yml`
- 한국 시간 월~토 07:10부터 16:10까지 매시간 실행. 전날이 미국 거래일이 아니면 관측을 건너뛴다.
- GitHub 예약은 지연될 수 있으므로 실제 요청 시작/종료 시각을 사용한다. 명목 예약 시각을 실제 수집 시각으로 기록하지 않는다.
- 매 실행 전체 보고서 대상(현재 553개)을 캐시 없이 query1/query2에 같은 기간으로 요청한다.
- 동시 HTTP 최대 4개, 서버별 1회, 한 회차 최대 1,106요청. 새 요청 시작 예산 15분, workflow 상한 30분.
- 관측이 중단돼 증거가 부족하면 `missing_observation`으로 기록하고 진단 작업을 실패 처리한다.
- 정상적인 결측 관측 자체는 진단 성공이다. 진단 workflow의 녹색 표시는 가격 전체 정상/보고서 게시를 의미하지 않는다.
- Actions Summary에 전체 정상 수와 시간 근거 상태를 표시한다. Artifact `price-availability-실행번호`에 아래 파일을 보존한다(90일).

| 파일 | 내용 |
| --- | --- |
| `summary.json` | 전체 종목 양쪽 서버 판정, 시각, 원본 경로, 오류 |
| `timing-report.json` | 거래일별/종목별 최초 정상 관측과 이전 결측, 정상 이후 재결측, 관측 간격, 잠정 권장 시각 |
| `raw/*.json` | 요청 URL/기간, HTTP 상태, 제한된 응답 헤더, 원본 SHA256, 처리 결과 |
| `raw/*.body.gz` | 변환 전 HTTP 응답 본문 |

일반 시장/매매 workflow도 실제 Yahoo 요청 원본을 `price-http-evidence/`에 저장한다.
일반 실행은 기존 Artifact 정책(30일)을 따르며, 정상 캐시 사용으로 HTTP 요청이 없으면 해당 응답 파일도 없다.
수집 코드의 진단 파일 쓰기 실패는 경고로 남기되 정상 가격의 게시 판단을 바꾸지 않는다.
쿠키·인증 헤더는 저장하지 않는다. raw 파일은 HTTP 라이브러리가 압축 해제한 변환 전 응답 본문이다.

## 판정과 시간 분석

- `raw_complete_parser_failed`: 원본에는 완성 OHLC가 있으나 기존 파서 실패. 저장된 오류와 원본으로 코드 처리 경로를 조사한다.
- `endpoint_readiness_difference`: 요청 시작 차이 5초 이내에 양쪽 서버의 원본 완성 여부가 다름.
- `both_incomplete`: 양쪽 모두 부족. 이후 정상 관측 시각과 비교한다.
- `transport_or_invalid_response`: HTTP 오류/잘못된 응답. 종가 결측과 별도로 취급한다.
- `comparison_time_skew`: 요청 시작 차이가 커서 동시 서버 비교로 단정하지 않는다.
- `parser_ready_raw_incomplete`: 수정종가 등으로 기존 파서는 통과하지만 원본 OHLC가 불완전하다. 양쪽 완성으로 처리하지 않는다.
- 준비 시각은 관측값이지 제공처 내부 갱신 시각이 아니다. 전체 종목도 한 순간이 아닌 수집 구간 동안 확인한다.
- 미국 장 마감 시각은 기존 XNYS 달력으로 구하고, 마감 후 경과 분과 한국 시각을 함께 기록한다.
- 최근 45일 중 최소 10개 서로 다른 미국 거래일에 전체 양쪽 서버 정상 관측이 있어야 잠정 권장값을 계산한다.
- 관측한 날짜 중 전체 정상 미확인 날짜 또는 정상 후 재결측 날짜가 있으면 권장값은 보류한다.
- 잠정값은 관측된 거래일별 최초 전체 정상의 가장 늦은 장 마감 후 경과 시간 + 30분이다.
- `provisionalMinutesAfterClose`: 장 마감 이후 실행 후보까지의 분. 서머타임/조기 폐장 대응은 이 값을 기준으로 한다.
- `provisionalKstTimeUsingLatestSessionClose`: 가장 최근 관측 거래일의 장 마감을 적용한 한국 시간 예시.
- 늦게 시작한 진단은 실제 준비 시각보다 늦은 상한을 줄 뿐이다. 관측 공백과 표본 수를 반드시 함께 확인해야 한다.
- 10일 정상이어도 미래 무결측을 보장하지 않는다. 자동으로 기존 게시 예약을 변경하지 않는다.

## 격리와 누적

진단은 가격 캐시/매매 원장/보고서 게시 포인터를 변경하지 않는다. 구성 종목 조회 캐시만 사용할 수 있다.
R2 `price-diagnostics/raw/`에 실행별 고유 ZIP, `price-diagnostics/observations/`에 실행별 시간 관측을 별도로 저장한다.
재실행도 고유 ID로 기존 증거를 덮어쓰지 않는다. 시간 계산은 최근 45일을 조회하며 원본은 자동 삭제하지 않는다.
공급자의 자체 갱신/서버 캐시 내부 원인은 이 관측만으로 확정할 수 없지만, 실제 응답 차이와 발생 시간대는 비교할 수 있다.

## 검증

- 로컬 전체 실요청: 2026-09-11 14:34:38~14:36:25 KST, 미국 9월 10일 대상 553개 양쪽 서버 모두 정상. 원본 1,106개 보존.
- 하루 관측으로 권장 시각을 제시하지 않는 상태 확인.
- Python 86개 세 시간대 통과, 프런트엔드/Worker 117개 및 빌드 통과.
- 원본 바이트 보존, 종가 null, 네트워크 오류, 서버 차이, 파서 실패 분리, 시각 차이 제한, 시간 증거 부족/재결측, 종목별 관측, R2 영역 격리, 예산 소진, 저장 실패 비간섭 검증.

## 운영 검증 완료

- 구현 커밋 `db83dda6aa37df5d31cafbbaade5df33275bbc44` main push 완료.
- [운영 진단 34566762225](https://github.com/east-house/rebalancing/actions/runs/34566762225) 성공.
- 실제 관측: 2026-09-11 14:39:18~14:42:02 KST. 미국 9월 10일 전체 553개 모두 `both_ready`.
- Artifact `price-availability-34566762225` (ID 10186352823)에서 HTTP 200 원본 1,106개 SHA256 일치 검증.
- R2 원본 ZIP: `price-diagnostics/raw/34566762225-c3fd2ae40bfb4a918a04d03b5af47b1d.zip`.
- R2 관측 저장 후 재조회한 시간 보고서: 관측 거래일 1개, 종목별 시간 항목 553개. 권장 시각은 `null`, 상태는 `insufficient_or_unstable_evidence`로 확인.
- `null`은 가격/날짜 오류가 아니라 최소 10거래일 시간 근거 미충족에 따른 보류다.
- 진단 전후 공개 시장 보고서 releaseId와 게시 코드 SHA 동일. 보고서 게시 상태를 변경하지 않았다.
- [세 시간대 운영 CI 34566750363](https://github.com/east-house/rebalancing/actions/runs/34566750363) 및 Cloudflare 빌드 검사 성공.
- 별도 수동 실행은 필요하지 않다. 기존 보고서 예약은 유지되며 신규 진단 예약이 다음 관측부터 독립적으로 누적한다.
