# 최신 가격 부족 재시도 검토 및 수정

## 실제 운영 검증 결과

코드 커밋 `c33db84`의 세 시간대 CI 및 배포는 성공했습니다. 프런트엔드/Worker 117개 테스트와 빌드도 통과했습니다.
그러나 [운영 실행 34420744629](https://github.com/east-house/rebalancing/actions/runs/34420744629)는
2026-09-10 보고서에 필요한 2026-09-09 가격이 계속 불완전하여 실패했습니다.

- 548종목의 자료 미준비 경고에 attempt=1/4부터 4/4까지 기록됐으며, 모두 마지막 시도까지 자료가 부족했습니다.
- AAPL은 응답 최신일 2026-09-09, 완성 가격 최신일 2026-09-08, 결측 항목 close로 네 번 동일했습니다.
  별도 읽기 전용 Yahoo 요청에서도 9월 9일 close=null을 확인했습니다.
- 전체 553개 중 최신 완성 자료는 5개로, 기존 최소 490개 검증을 통과하지 못했습니다.
- 수집 단계는 09:19:25~09:38:16 KST, 약 18분 51초였습니다. 많은 종목이 동시에 부족할 때 종목별 대기로
  실행 시간이 크게 늘어나는 운영 비용이 확인됐습니다. 짧은 시간 내 복구가 보장되는 변경은 아닙니다.
- 9월 9일 기존 정상 시장·포트폴리오 보고서는 실행 전후 응답 바이트가 동일했고,
  9월 10일 실패 상태는 별도로 보존됐습니다. 오래된 자료를 새 보고서로 게시하지 않았습니다.

따라서 재시도·중단·기록 보존 동작 검증은 완료됐지만, 공급처의 지속적인 원자료 부족이나
오늘 보고서 생성 자체까지 해결됐다고 판단할 수 없습니다. 이 결과 확인 후 추가 로직 변경은 하지 않았습니다.

추가 발견: 이전 커밋의 [매매 예약 실행 34385292314](https://github.com/east-house/rebalancing/actions/runs/34385292314)는
가격 수집 이전 apt-get update 단계에서 Google Chrome 패키지 저장소의 Hash Sum mismatch로 exit 100 종료했습니다.
이번 가격 재시도와 별개의 실행 환경 오류이며, 설치 코드는 수정하지 않았습니다.

## 검토 결론

별도 재시도 루프를 중첩하면 요청량과 실행 시간이 곱으로 늘 수 있습니다.
따라서 기존 Yahoo 수집기의 종목당 시도 한도 안에 가격 준비 여부 검증을 넣었습니다.
현재 설정에서는 통신 실패와 가격 부족을 합쳐 최대 4회 요청합니다. 상한 없는 반복은 없습니다.
부족한 종목은 추가 요청으로 처리 시간이 늘 수 있지만 최대 동시 요청 수 4개는 유지합니다.

## 변경 범위

- `support._download_yahoo_frame`에 선택적 `expected_latest`를 추가했습니다.
- 고정된 기대 거래일에 date/open/high/low/close가 완성된 행이 없으면 `PriceDataNotReady`로 재시도합니다.
- 기존 지수형 지연과 jitter, query2/query1 교대, 통신 타임아웃을 그대로 사용합니다.
- 정상 캐시가 있는 종목은 호출하지 않으며, 정상 응답을 얻은 종목은 즉시 반환합니다.
- 기대 거래일이 지정되지 않은 기존 호출 방식은 변경하지 않습니다.
- 요청 날짜 범위는 재시도 도중 바뀌지 않습니다. 미래 행으로 과거 거래일 자료를 대신하지 않습니다.
- 실패 로그에 기대 거래일, 응답 최신일, 완성 가격 최신일, 결측 항목과 시도 횟수를 남깁니다.
- 재시도 소진 시 기존 캐시 대체와 전체 신선도 검증으로 돌아갑니다. 오래된 캐시를 최신으로 표시하지 않습니다.

## 변경하지 않은 로직

한국 보고일/XNYS 달력, 휴장일 처리, 최소 최신 종목 수 490개, 기준 지수의 신선도,
전략 계산, 체결·원장 갱신, 불변 객체 게시, 과거 기록 복구 분리, 예약 재시도 일정은 그대로입니다.
가격 다운로드/검증 이전에는 원장과 공개 보고서를 변경하지 않습니다.

## 검증

- UTC, America/New_York, Asia/Seoul 각각 Python 전체 69개 통과.
- 추가 8개 사례: 오래된 응답 후 정상화, OHLC 결측 후 정상화, 미래 행만 있는 응답 차단,
  통신 오류와 자료 부족의 공통 4회 상한, 기존 선택적 인자 없는 호출 호환,
  정상 응답 불필요 재시도 없음, 부족한 종목만 재수집 후 정상화, 끝내 실패 시 캐시 바이트 보존.
- 기존 게시 실패·동시 갱신·휴장일 원장 보존·과거 자료 분리 테스트도 함께 통과했습니다.
- 가격 지연/통신 실패는 가짜 HTTP 응답으로 재현했습니다. 실제 제공처의 장애를 인위적으로 발생시키지는 않았습니다.

## 운영 한계

제공처가 계속 자료를 주지 않으면 같은 실행에서 해결할 수 없습니다. 이 경우 기존 검증이 게시를 막고,
이전 정상 보고서와 실패 기록을 유지한 뒤 다음 예약 실행이 재시도합니다.
당시 Yahoo 원시 응답이 보존되지 않은 2026-09-09 사건의 공급자 내부 원인을 새로 확정한 것은 아닙니다.
과거 뉴스 부족 2건을 복구하는 변경도 아닙니다.
