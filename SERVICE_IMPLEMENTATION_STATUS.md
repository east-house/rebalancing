# Balance 서비스 전체 기능 및 구현 현황

기준일: 2026-07-26  
ETF 연구 데이터 기준일: 2026-07-24  
데이터 버전: `20260724.92bc7c6c`

## 1. 최종 구현 결론

현재 서비스는 기존의 로컬 포트폴리오·리밸런싱 도구에 다음 세 기능을 실제로 추가한 상태입니다.

1. 국내 상장 ETF 분석 및 두 ETF 비교
2. 자산군 우선 방식의 설명 가능한 포트폴리오 생성
3. 후보 포트폴리오의 위험지표 및 리밸런싱 백테스트

계좌연동, 사용자 계정, 자동주문, 승인형 주문, 증권사 API와 서버 데이터베이스는 추가하지 않았습니다. 모든 연구 계산은 사용자 브라우저에서 실행되며, 서버는 버전이 고정된 공개 ETF JSON만 전달합니다. 따라서 현재 Cloudflare Workers Static Assets + R2 + GitHub Actions 구조에서 무료 운영을 유지할 수 있는 형태입니다.

기술적으로는 지금 배포할 수 있습니다. 다만 공개·상업 서비스 전에는 현재 수집 소스의 데이터 이용 및 재배포 조건을 확인하거나, 계약·공식 API로 교체해야 합니다. 이 문제는 코드 오류가 아니라 운영 허가의 문제이므로 배포 전 확인 항목으로 남아 있습니다.

## 2. 사용자가 이용하는 전체 서비스

### 기존 포트폴리오 도구

- 한국·미국 주식 및 ETF 15,938개 정적 종목 검색
- 복수 포트폴리오 생성, 이름 변경, 보유수량·평균단가·목표비중 입력
- R2 종가를 이용한 원화 평가와 키움 일반 온라인 기준 예상 거래비용 계산
- 현금을 포함한 목표비중 검증과 정수 주식 리밸런싱 미리보기
- 신규 투자금의 목표비중별 매수 수량 계산
- 보유수량 고정 방식의 최근 1년 총자산 추이
- 입력 정보의 브라우저 `localStorage` 저장과 사용자 직접 삭제
- 서버에 계좌정보·수량·평균단가·목표비중을 저장하지 않는 로컬 우선 구조

기존 상단의 `ETF 연구소` 버튼으로 새 기능에 진입하고, 연구소의 `포트폴리오로` 버튼으로 돌아옵니다.

### 3단계: ETF 분석

관리형 카탈로그는 국내 상장 ETF 50개이며 이 중 기본 생성기 사용 대상은 45개입니다. 국내주식, 해외주식, 신흥국주식, 채권, 현금성, 금, 리츠를 포함합니다.

제공 기능:

- ETF명·종목코드·전략명 검색
- 자산군 필터
- 최신가, 순자산가치(NAV), 괴리율, 시가총액, 최근 20일 평균 거래대금 표시
- 1년 가격수익률, 1년 연환산 변동성, 하방변동성, 최근 3년 최대낙폭 계산
- 동일 노출 ETF 내 설명 가능한 품질점수 계산
- 두 ETF의 구성종목 중복도와 일별 가격수익률 상관계수 분리 표시
- 공통 구성종목과 각 공통종목의 최소 비중 표시
- A/B/C 데이터 등급과 생성기 사용 가능 여부 표시
- 사용자가 전체 구성종목 CSV를 불러와 현재 브라우저 메모리에서만 보완 가능

품질점수의 기본 가중치는 다음과 같습니다.

| 항목 | 가중치 |
| --- | ---: |
| 최근 20일 거래대금 | 30% |
| 총보수 | 25% |
| NAV 괴리 안정성 | 20% |
| 시가총액과 가격 이력 | 15% |
| 데이터 품질 | 10% |

자료가 없는 점수 항목은 0점으로 단정하지 않고, 확인 가능한 항목의 가중치를 다시 정규화합니다.

구성종목 중복도는 공통 구성종목에 대해 `min(ETF A 비중, ETF B 비중)`을 합산합니다. 양쪽 구성자료 범위가 각각 90% 이상일 때만 `전체 구성 기준`으로 표시합니다. 그보다 작으면 `공개된 부분 구성 기준`, 구성비중 자체가 없으면 `계산 불가`로 표시합니다. 이 구분 덕분에 일부 공개 자료를 전체 중복도처럼 오인하지 않습니다.

CSV 헤더는 다음 이름을 지원합니다.

```text
etf_ticker,holding_key,holding_name,weight
069500,005930,삼성전자,28.50
```

CSV는 업로드 서버로 전송하지 않고 브라우저 탭의 메모리에서 현재 세션 동안만 사용합니다.

### 4단계: 포트폴리오 생성

이 기능은 먼저 특정 ETF를 고르는 방식이 아니라 다음 순서로 동작합니다.

```text
1. 사용자가 자산 노출과 목표비중을 확정
2. 동일한 노출을 가진 ETF만 후보군으로 구성
3. 유동성·비용·NAV·규모·이력·자료품질 필터 적용
4. 가능한 ETF 조합 생성
5. ETF당 최대비중과 두 ETF 간 최대 중복도 적용
6. 균형·저비용·저중복 관점의 후보 3개 제시
7. 선정 종목, 점수, 목표비중, 예상 정수 수량과 잔여현금 표시
```

기본 프리셋:

- 방어형 예시: 국내·미국주식 30%, 채권 60%, 현금성·금 10%
- 균형형 예시: 국내·미국·선진국주식 60%, 채권 35%, 금 5%
- 성장형 예시: 국내·미국·신흥국주식 90%, 채권 5%, 금 5%

프리셋은 투자 추천이 아니라 기능을 시작하기 위한 비개인화 예시입니다. 사용자는 다음을 직접 변경할 수 있습니다.

- 자산 노출 추가·삭제
- 자산 노출별 목표비중
- 투자금액
- ETF당 최대비중
- 두 ETF 간 최대 구성 중복도
- 최근 20일 최소 거래대금
- 직접 보유한 개별 주식의 이름과 전체 포트폴리오 내 비중

직접 보유 주식은 ETF 매수수량 계산 대상이 아니라 실질 기업 노출과 전체 비중 합계에만 반영합니다. ETF 자산군과 직접 보유 주식 비중의 합계가 100%가 아니면 후보를 만들지 않습니다. 목표비중이 0%인 자산 노출은 후보에서 제외합니다.

후보별 제공 정보:

- ETF와 자산군
- 목표비중과 최신가 기준 예상 정수 수량
- 품질점수와 선정 근거
- 가중평균 총보수
- 가중평균 구성 중복도와 계산 신뢰도
- 데이터 신뢰도 점수
- ETF 매수예산 내 예상 잔여현금
- 공개 구성자료로 계산 가능한 상위 실질 기업 노출
- 부분 구성자료와 비용 누락에 대한 경고

구성자료가 부분 공개인 쌍의 중복도는 강제 탈락 조건으로 사용하지 않고 경고와 신뢰도에 반영합니다. 확인되지 않은 값을 정확한 수치로 가정해 좋은 조합처럼 보이게 하지 않기 위한 정책입니다.

### 5단계: 위험 및 백테스트

선택한 포트폴리오 후보를 다음 세 방식으로 동시에 계산합니다.

- 리밸런싱하지 않는 매수 후 보유
- 분기 리밸런싱
- 연간 리밸런싱

공통 조건:

- 모든 선택 ETF에 가격이 존재하는 공통 거래일만 사용
- 후보 내 ETF 목표비중의 합을 100%로 재정규화
- 소수점 수량을 허용하는 지수형 백테스트
- 리밸런싱 시 양방향 회전율의 절반을 사용
- 기본 거래비용 5bp를 회전율에 곱해 포트폴리오 가치에서 차감
- 무위험수익률 기본값 2.5%

계산 지표:

- 누적수익률
- 연환산수익률
- 연환산 변동성
- 최대낙폭(MDD)
- 하방변동성
- 샤프지수
- 소르티노지수
- 최악의 월 수익률
- 리밸런싱 횟수
- 누적 회전율

세 전략의 자산가치 곡선과 지표 표를 함께 보여줍니다. 상장 전 대체지수 연결, 생존편향 보정, 세금, 실제 호가 스프레드와 체결 슬리피지는 포함하지 않습니다.

현재 50개 ETF 모두 `price` 수익률 자료이고 `totalReturnCount`는 0입니다. 즉 분배금 재투자가 보장된 총수익률이 아닙니다. 서비스 화면과 결과 경고에 이 제한을 명시했습니다.

## 3. 데이터와 갱신 구조

### 현재 스냅샷

| 항목 | 현재값 |
| --- | ---: |
| ETF 수 | 50개 |
| 생성기 대상 | 45개 |
| 가격 기준일 | 2026-07-24 |
| 가격 이력 시작 | 최대 2016-01-01 |
| 분석 JSON | 약 50KB |
| 가격 이력 JSON | 약 3.56MB |
| 가격 이력 gzip | 약 497KB |
| 총수익률 ETF | 0개 |

최초 스냅샷을 `public/data`에 포함하므로 R2가 비어 있거나 일시 장애여도 정적 자료로 기능합니다. 운영 중에는 주간 GitHub Actions가 같은 데이터를 새 버전으로 만들고 R2에 게시합니다.

데이터 빌더가 수집·계산하는 내용:

- FinanceDataReader의 국내 ETF 목록과 가격 이력
- 공개 상품 화면의 운용사, 기초지수, 총보수와 표시 가능한 구성정보
- 최신가, NAV, 시가총액
- 가격수익률과 위험지표
- 데이터 등급 및 생성기 제외 사유

버전은 `최신가격일.설정과빌더해시` 형식입니다. 빌더 로직이나 관리 종목 설정이 바뀌면 같은 가격 기준일에도 새 버전이 생기므로 이미 캐시된 불변 파일을 덮어쓰지 않습니다.

### R2 객체와 Worker API

```text
etf-research/latest/manifest.json
etf-research/versions/{version}/analysis.json.gz
etf-research/versions/{version}/returns.json.gz
```

```text
GET /api/etf-research/manifest
GET /api/etf-research/versions/{version}/analysis
GET /api/etf-research/versions/{version}/returns
```

Worker는 gzip 객체를 스트리밍 해제해 JSON으로 전달합니다. R2 객체가 없거나 R2 호출이 실패하면 배포된 정적 스냅샷으로 자동 전환합니다. 버전 파일은 1년 불변 캐시, manifest는 짧은 캐시를 사용합니다.

브라우저는 manifest를 먼저 받고 해당 버전의 분석 및 가격 묶음을 IndexedDB에 저장합니다. 다음 방문에 같은 버전이면 다시 다운로드하지 않습니다. 가격 이력은 비교 상관계수나 백테스트를 요청할 때만 지연 로드합니다.

### 자동 갱신

`.github/workflows/update-etf-research.yml`은 매주 금요일 23:20 KST에 실행되며 수동 실행도 지원합니다.

```bash
npm run research:update
```

워크플로는 외부 페이지를 사용자 요청 시 호출하지 않고, 유지보수 시점에 한 번 수집해 R2에 게시합니다. 로컬 최초 생성은 약 22초였으며 주 1회 실행은 GitHub Free Actions 시간에 부담이 매우 작습니다.

## 4. 무료 운영 적합성

현재 구조에서 무료 운영을 방해하는 기술 요소는 발견되지 않았습니다.

- 사용자 요청마다 외부 금융 API를 호출하지 않음
- Worker에서 최적화나 백테스트를 실행하지 않음
- 사용자 계정과 서버 데이터베이스가 없음
- ETF 분석은 manifest와 약 6KB gzip 분석 묶음 1회 요청
- 가격 이력은 필요할 때 약 497KB gzip 묶음 1회 요청
- 같은 버전은 브라우저 IndexedDB와 Cloudflare 캐시 재사용
- 정적 자산 요청은 Workers 계산 요청과 분리
- 주 1회 GitHub Actions 작업
- R2에는 버전당 압축 약 0.5MB만 추가

현재 50개 범위를 유지하면 ETF 연구 기능 때문에 Workers 일일 요청, R2 무료 저장공간 또는 GitHub Free 실행시간을 초과할 현실적인 가능성은 낮습니다. 다만 실제 트래픽과 객체 누적량은 Cloudflare·GitHub 대시보드에서 월 1회 확인해야 합니다.

ETF 수를 수천 개로 확대하거나 전체 구성종목을 매일 보관하면 무료 구조가 달라집니다. 현재 구현은 의도적으로 50개 관리형 카탈로그, 주간 갱신, 브라우저 계산을 유지합니다.

## 5. 확인된 한계와 운영 전 조치

### 5.1 구성종목 자료는 부분적임

공개 기본 화면은 일부 ETF에 상위 구성종목만 제공하고, 해외자산·채권 ETF는 구성비중을 제공하지 않는 경우가 많습니다. 현재 스냅샷에서 일부 국내 ETF는 공개된 3개 종목 수준이고 여러 해외 ETF는 비중 합계가 0%입니다.

개선 조치:

- 90% 미만 자료는 `전체 중복도`라고 표시하지 않음
- 부분 자료는 강제 탈락 조건으로 사용하지 않음
- 사용자 보유 전체 구성 CSV를 로컬에서 보완 가능
- 화면에 공개범위와 신뢰도를 항상 함께 표시

공개 서비스에서 정확한 ETF 중복도를 핵심 상품으로 내세우려면 운용사·거래소의 전체 PDF/CSV를 정식으로 수집하거나 허가된 데이터 공급원을 확보해야 합니다.

### 5.2 분배금 포함 총수익률이 아님

현재 가격 이력은 분배금 재투자를 보장하지 않습니다. 따라서 분배금이 큰 ETF의 장기성과를 과소평가할 수 있습니다.

개선 조치:

- manifest와 종목별 시계열에 `returnMode`, `distributionIncluded` 저장
- 백테스트 화면에 가격수익률 경고 상시 표시
- 총수익률과 가격수익률을 조용히 혼합하지 않음

총수익률 공급원을 확보하면 기존 스키마를 유지한 채 ETF별로 `total-return`을 게시할 수 있습니다.

### 5.3 데이터 이용·재배포 조건

현재 빌더는 FinanceDataReader와 공개 상품 화면을 이용합니다. 기술적으로 동작하지만, 공개·상업 서비스에서 결과 파일을 재배포할 수 있는지는 소스별 약관과 권리를 별도로 확인해야 합니다.

운영 원칙:

- 권리 확인 전에는 내부 테스트 또는 제한된 베타로 취급
- 출처와 수집시각 유지
- 공개 전 공식 KRX API, 운용사 파일 또는 계약된 공급원으로 교체 가능한 빌더 구조 유지
- 확인되지 않은 실시간성·정확성·완전성을 광고하지 않음

### 5.4 투자 추천으로 오인될 가능성

생성 결과는 사용자 입력 조건을 기계적으로 비교한 비개인화 예시입니다. 그러나 특정 ETF와 수량을 제시하므로 공개 운영 문구와 기능 범위는 별도 법률 검토 대상이 될 수 있습니다.

현재 완화 조치:

- 수익 보장 표현 없음
- 선정 공식, 자료 누락, 위험과 백테스트 한계를 동시에 표시
- 사용자가 자산군·비중·제약을 직접 결정
- 계좌연동과 주문 기능 없음
- 결과가 과거 검증이며 투자 추천이 아님을 표시

면책 문구만으로 규제 문제가 자동 해결되는 것은 아니므로 공개 상용화 전 국내 금융 규제 검토가 필요합니다.

### 5.5 예약 작업 운영

GitHub 예약 작업은 저장소 상태나 플랫폼 사정으로 지연·비활성화될 수 있습니다. ETF 분석은 최신 실시간 기능이 아니므로 서비스 장애로 이어지지 않고 마지막 정상 버전과 정적 스냅샷을 계속 사용합니다. 월 1회 `generatedAt`과 Actions 성공 여부를 확인해야 합니다.

## 6. 구현 파일

| 영역 | 주요 파일 |
| --- | --- |
| 연구 화면 | `src/features/etf-research/EtfResearchLab.tsx` |
| 연구 화면 스타일 | `src/features/etf-research/etfResearchLab.css` |
| 분석 계산 | `src/domain/etfAnalytics.ts` |
| 포트폴리오 생성 | `src/domain/portfolioGenerator.ts` |
| 백테스트 | `src/domain/etfBacktest.ts` |
| 타입과 데이터 계약 | `src/domain/etfResearchTypes.ts` |
| API 로더 | `src/api/etfResearch.ts` |
| IndexedDB 캐시 | `src/storage/etfResearchCache.ts` |
| 관리형 50개 설정 | `config/etf-research-universe.json` |
| 데이터 빌더 | `scripts/build_etf_research_data.py` |
| 주간 갱신 | `.github/workflows/update-etf-research.yml` |
| Worker API | `worker/index.js` |
| 최초 정적 데이터 | `public/data/etf-research-*.json` |

## 7. 검증 결과

2026-07-26 기준 결과:

```text
TypeScript typecheck: 통과
Vitest: 14개 테스트 파일, 73개 테스트 통과
Python 데이터 파이프라인 unittest: 23개 테스트 통과
ETF 연구 데이터 빌더 py_compile: 통과
Vite production build: 통과
Cloudflare Wrangler deploy --dry-run: 통과
Worker gzip bundle: 약 2.11KB
브라우저 JS bundle: 약 94.13KB gzip
브라우저 CSS bundle: 약 10.06KB gzip
ETF 가격 이력: 약 497KB gzip
```

Wrangler가 로컬 샌드박스 외부 로그 경로에 기록하지 못했다는 EPERM 메시지는 있었지만, 드라이런 자체는 `--dry-run: exiting now`와 종료코드 0으로 완료되었습니다. 배포 설정이나 Worker 번들 오류는 아닙니다.

테스트 범위:

- 구성종목 중복도와 실질 기업 노출
- 가격수익률 상관계수
- ETF 품질점수 기반 포트폴리오 후보 생성
- 비중 합계·필터·후보 3안 검증
- 리밸런싱 백테스트와 누락 이력 오류
- 배포 스냅샷 50개 종목·버전·가격 집합 정합성
- 방어형·균형형·성장형 세 프리셋 생성 가능 여부
- 실제 배포 스냅샷 백테스트 가능 여부
- ETF 연구 화면 로딩과 후보 생성 사용자 흐름
- Worker R2 gzip 처리와 정적 fallback

## 8. 배포 및 운영 순서

1. 데이터 소스의 공개 재배포 가능 여부를 확인합니다.
2. GitHub의 기존 R2 Secrets가 `update-etf-research.yml`에도 사용 가능한지 확인합니다.
3. 필요하면 Actions에서 `Refresh managed ETF research data`를 수동 실행합니다.
4. `npm run deploy`로 Cloudflare Worker와 정적 자산을 배포합니다.
5. 배포 후 ETF 연구소에서 KODEX 200 검색, 균형형 후보 생성, 백테스트를 한 번 확인합니다.
6. 월 1회 Actions 성공, R2 사용량, Workers 요청량, 데이터 기준일을 확인합니다.

실제 배포 명령은 외부 서비스를 변경하므로 이번 구현에서는 실행하지 않았습니다. 코드, 빌드, 데이터 스냅샷, R2 게시 워크플로와 배포 드라이런까지 완료된 상태입니다.

## 9. 최종 상태

현재 구현은 무료 서비스 목적에 맞는 최종 MVP입니다.

- 기능 완성도: ETF 분석, 자산군 우선 생성기, 위험·백테스트 사용 가능
- 기술 배포 준비: 완료
- 무료 운영 구조: 적합
- 계좌·주문 기능: 의도적으로 없음
- 정확한 전체 ETF 중복도: 전체 구성자료가 있는 경우에만 가능
- 총수익률 백테스트: 데이터 공급원 확보 전까지 제한
- 공개·상업 운영: 데이터 재배포 조건과 금융 규제 확인 후 진행

따라서 현재 상태는 `기능 구현 완료 / 무료 배포 준비 완료 / 공개 운영 권리 확인 필요`로 정리할 수 있습니다.
