# 날짜 및 기록 신뢰성 개선 완료 보고서

2026-09-10 정정: 아래의 9월 9일 원래 실패 Actions 직접 예외 미확정 상태는 해소됐습니다.
인증된 artifact로 최신 가격 종목 수 부족(`6 < 490`)을 확인했습니다.
[원인 확인 보고서](./ACTIONS_FAILURE_RCA_2026-09-10.md)에 상세 근거와 확인 한계를 정리했습니다.
후속 가격 수집 재시도 보완은 [검토·수정 보고서](./PRICE_READINESS_RETRY_REVIEW_2026-09-10.md)에 정리했습니다.

## 후속 수정: 일일 갱신과 과거 복구 분리

실행 34363316404는 2026-09-09 시장·포트폴리오를 정상 게시했지만 2026-08-18,
2026-09-03의 뉴스 0건 검증 실패로 전체 실패했습니다. 아래 최초 전환 안내의
"기본 입력으로 모든 누락일 복구" 동작은 다음과 같이 변경했습니다.

- 아침 일일 작업은 고정된 `dailyStartDate` 이후 누락분만 처리합니다. 기준일은 최초 한 번만 저장하여
  이후 최신 보고서가 성공해도 중간의 일일 실패가 자동 재시도 대상에서 사라지지 않습니다.
- 이전 누락일은 `recoveryDates`에 보존하고 기존 failed 상태·원인을 삭제하거나 성공으로 바꾸지 않습니다.
- `recover_history=true` 수동 실행은 과거 복구만 수행합니다. 해당 실패는 그 복구 실행을 실패 처리합니다.
- 뉴스 수량·날짜·게시 무결성 검증을 완화하지 않았습니다. 과거 원본 없는 기록을 복구했다고 주장하지 않습니다.
- 운영 화면은 일일 게시 결과와 과거 복구 미완료 날짜를 각각 표시합니다.
- Python 61개, 프런트엔드·Worker 117개 및 프로덕션 빌드 검증을 통과했습니다.
- 과거 2개 날짜의 자료 복구 자체는 미완료입니다. 이 후속 수정은 이 문제가 매일의 갱신을 방해하지 않게 하는 수정입니다.

## 적용 범위

2026-09-09 기준 코드 개선 보고서입니다. 이전 진단은
[원인 조사](./REPORT_HISTORY_TIMEZONE_AUDIT_2026-09-09.md), 이전 커밋을 반영한 설계는
[개선 계획](./DATE_RELIABILITY_PLAN_2026-09-09.md)에 있습니다.
코드 push, Cloudflare 배포, R2 데이터 복구는 서로 다른 단계입니다.

## 이전 수정과 이번 수정의 차이

이전에는 실행 시각 보정, 조기 게시 방지, 캐시 URL 및 데이터 신선도 검사를 수정했습니다.
이번에는 생성기마다 날짜를 다시 추측하는 구조와 여러 파일을 따로 덮어쓰는 게시 구조를 변경했습니다.

- 한국 보고일, 미국 거래일, 예정 게시 시각, 실제 생성 시각, 데이터 마감 시각을 분리했습니다.
- XNYS 달력으로 휴장일·서머타임·다음 거래일을 계산하며 과거 재실행에 미래 가격이 섞이지 않도록 제한했습니다.
- 아침 시장·포트폴리오 보고서는 날짜와 거래일이 일치하고 preview가 아닐 때만 함께 게시합니다.
- 보고서·목록·원장을 SHA-256 객체로 저장하고 업로드를 다시 읽어 검증한 뒤, 단일 current manifest를 조건부 교체합니다.
  도중 실패하면 이전 정상 공개 묶음이 유지됩니다. 동시 갱신 충돌도 이전 상태를 덮어쓰지 않습니다.
- 설계안의 별도 Worker 쓰기 인증 대신 기존 S3/R2 인증과 조건부 PutObject를 사용합니다. 새로운 secret은 필요하지 않습니다.
- IRCS의 같은 거래일 재실행은 기존 체결 결과와 생성 시각을 그대로 보존합니다. 원장이 처리한 날짜의 원본이 없으면 명시적으로 실패합니다.
- 원장 초기화·전략 변경 전 이전 원장을 보관하며 과거 날짜로 최신 원장을 되돌리는 재구축을 차단합니다.
- 누락 보고일을 오름차순 재처리하고 실패를 별도로 기록합니다. 재시도는 아침 07:30/09:30/11:30, 매매 19:13/21:13/23:13 KST입니다.
- 시장 원본은 남아 있지만 목록에서 사라진 날짜를 복구합니다. 기존 생성 시각을 이유로 목록을 삭제하는 규칙을 제거했습니다.
- 포트폴리오 보고서를 날짜별 보관하며 과거 조회는 읽기 전용입니다. 개인 기록의 180건 자동 삭제를 제거하고 JSON 백업 내보내기를 추가했습니다.
- 서버 게시 상태를 표시하고 60초 간격·화면 복귀 시 갱신합니다. 목록과 본문은 release를 맞추며 충돌 시 혼합본 대신 오류를 반환합니다.
- 운영 저장소 오류를 정적 스냅샷으로 감추지 않습니다. 기본 자산 추이와 현재 보유수량 시뮬레이션을 구분했습니다.

## 검증

- Python: UTC, Asia/Seoul, America/New_York 각각 59개 테스트 통과.
- 프런트엔드 및 Worker: npm lockfile 기준 115개 테스트 및 TypeScript/Vite 빌드 통과.
- 핵심 회귀: 휴장일·서머타임·윤년·자정 경계, 48시간 지연, 미래 캐시 차단, 누락 목록 복구,
  게시 단계별 실패, 동시 게시 충돌, 같은 거래일 중복 체결 방지, 과거 원장 역행 방지, 장애 로그 보존.
- 실제 Chrome: 미국 시간대에서 1440x1000 및 390x844 화면 검증.
  과거 조회 시 개인 기록 불변, 적용 버튼 비활성화, 휴장일 상태, 가로 넘침 없음, 페이지 오류 없음 확인.
- R2 장애·충돌 테스트는 가짜 S3 저장소에 장애를 주입한 검증입니다. 운영 R2에 시험 데이터를 쓰지는 않았습니다.
- 새 `Verify report dates and integrity` Actions가 향후 push/PR마다 세 시간대의 테스트·빌드를 실행합니다.

## 운영 전환 순서

1. main push 후 Git 연동 Cloudflare 배포가 완료되었는지 확인합니다. 새 Worker와 UI가 먼저 적용되어야 합니다.
2. `/api/market-reports/status`, `/api/portfolio-reports/status`, `/api/trading-test-reports/status`를 확인합니다.
   최초 게시 전에는 releaseId가 null일 수 있습니다. 새 게시 후 codeSha와 releaseId가 나와야 합니다.
3. `Generate and publish daily US market report`를 수동 실행하고 `repair_index=true`를 선택합니다.
   원본이 남은 과거 보고서 목록만 복구하며 시세를 새로 수집하지 않습니다.
4. 같은 워크플로를 기본 입력으로 실행하면 누락 보고일을 생성·검증·게시합니다.
   특정 과거 보고서를 다시 만들 때만 `as_of=YYYY-MM-DD`, `revise=true`를 사용합니다.
5. 매매 워크플로는 기본 입력으로 실행합니다. `reset_ledger`는 일반 재시도에 사용하지 않습니다.
   과거 잘못 덮어쓴 체결 내역의 복구는 원본·입력 자료를 확인한 후 별도 검증된 재구축으로 처리합니다.
6. Actions 성공, status의 실패 여부, 시장·포트폴리오의 동일 거래일, 과거 날짜 목록,
   휴장일 매매 원본 불변을 확인합니다. 장애 시 Actions artifact의 daily.log와 failures.json을 확인합니다.

## 정상 결과와 한계

- 월요일이나 미국 휴장일 다음 한국 평일에는 서로 다른 한국 보고일에 같은 미국 거래일이 나올 수 있습니다. 정상입니다.
- 매매 테스트는 미국 거래일이 늘지 않으면 마지막 보고서 날짜를 유지하고 신규 거래일 없음 상태만 기록합니다.
- 지연 실행은 해당 보고일로 처리됩니다. 새로 만든 과거 아침 보고서는 당시 원본이 아니라 재구성본으로 표시합니다.
- 새 게시 전 실패하면 지난 정상 자료를 유지합니다. 상태 기록까지 실패하는 R2 전체 장애는 UI 오류와 Actions 로그로 드러납니다.
- 기존에 삭제된 개인 localStorage 기록이나 원본 없는 과거 보고서는 이번 코드만으로 정확히 복원할 수 없습니다.
  개인 기록은 여전히 기기별 저장이며 클라우드 동기화·자동 복원·백업 가져오기는 구현하지 않았습니다.
- 입력 자료와 코드 SHA는 새 게시부터 보관합니다. 입력을 이용한 완전 자동 과거 재현 도구나 별도 ledgerId 체계는 이번 범위가 아닙니다.
- 기존 시장 index의 원본 복구와 포트폴리오 날짜별 신규 보관을 구분해야 합니다. 원본이 없는 옛 포트폴리오 기록을 원본인 것처럼 만들지 않습니다.
- 9월 9일 실패 Action의 실패 단계는 확인했지만 인증이 필요한 상세 로그를 확보하지 못해 당시 예외는 확정하지 못했습니다.
  이번 개선을 그 예외의 해결 증명으로 간주하지 않습니다. 이후 실패는 진단 artifact와 요약에 원인을 남기도록 강화했습니다.
- 외부 시세 장애, GitHub 예약 실행 누락, 브라우저 데이터 삭제까지 없애는 보장은 하지 않습니다.
  목표는 날짜 오류·중복 처리·부분 게시를 차단하고, 실패를 감지하여 원본을 보존한 상태로 재시도하는 것입니다.

## 롤백 주의

새 게시 이후에는 예전 개별 업로드 스크립트나 예전 Worker로 단순 회귀하지 않습니다.
현재 게시 포인터와 이전 release manifest를 확인하고 관련 데이터 묶음을 함께 복원해야 합니다.
기존 논리 경로의 옛 파일만 읽으면 최신 불변 게시를 놓칠 수 있습니다.
