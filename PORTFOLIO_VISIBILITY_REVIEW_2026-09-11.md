# 포트폴리오 표시 수정 및 매매 차이 재점검

> 2026-09-13 업데이트: 아래 과거 매매 불일치는 원본 보존 후 기준 원장을
> 개정하고 9거래일 일치 검증을 완료했습니다. 현재 상태는
> [복구 완료 보고서](./HISTORY_REPAIR_2026-09-13.md)를 참고하세요. 아래는 당시 조사 기록입니다.

## 포트폴리오 표시

- 실제 사이트에서 1440/768/390/320px, 최초 화면과 초기 계좌 저장 후 화면을 검사했다. 보고서 하단 및 footer가 사라지는 오류나 JavaScript 예외는 재현되지 않았다.
- 확인한 문제 1: `history.slice(-8)` 때문에 브라우저에 저장된 기록 중 최근 8개만 표시했다. 전체 기록을 역순으로 표시하도록 수정했다. 저장 자료와 날짜는 변경하지 않는다.
- 확인한 문제 2: 모바일에서 표의 최소 너비가 화면보다 커서 오른쪽 열을 보려면 가로 스크롤이 필요했다. 980px 이하에서는 모든 열을 항목명과 함께 세로로 펼쳤다. 초기 매수안, 오늘 행동, 현재 모델 포트폴리오의 모든 값이 표시된다.
- 12개 기록을 넣고 가장 오래된 기록까지 선택 가능한지, 기존 저장 데이터가 보존되는지 테스트를 추가했다.
- 프런트엔드/Worker 129개 테스트 및 빌드 통과. 브라우저에서 4개 폭과 초기/저장 후 8개 조합을 검사했다. 모든 표의 표시 너비와 전체 너비가 일치한다.
- 사용자가 말한 잘림 위치는 추가 질문했으며 아직 구체적인 위치나 화면 캡처를 받지 못했다. 위 두 가지 재현 가능한 표시 문제를 수정한 것이며, 확인되지 않은 다른 잘림까지 해결했다고 주장하지 않는다.

## 날짜별 서버 보고서와 기기 기록의 구분

- 조회한 서버 날짜 목록은 9월 9일~11일 3개다. 9월 8일 API는 404를 반환한다. 이는 최근 8건 표시 제한과 별개다.
- 이번 변경은 날짜별 서버 보고서를 생성하거나 복구한 것이 아니다. 날짜 목록 누락을 화면 잘림으로 오인해 임의 날짜·가상 기록을 추가하지 않았다.
- 기기 기록은 해당 브라우저 localStorage에만 있다. 삭제된 기기 기록을 서버에서 복구할 수는 없다.

## 매매 결과 추가 검사

- 동일 자료에서 기존 정기 엔진을 8거래일 순차 실행한 결과와 재현 엔진 결과가 같은 사실은 유지된다.
- 9월 2일의 WMT, NKE, EXC, ROST, TFC 지표를 추가 검사했다. 검증된 전체 가격과 누락 복구 전 기본 입력, 9월 9일 실행 artifact의 입력을 각각 대입했다. 세 경우 모두 WMT/NKE 진입 신호는 false, EXC/ROST/TFC는 true였다.
- 따라서 앞서 보완한 786건의 누락만으로 기존 WMT/NKE 매수와 새 검증의 차이를 설명할 수 없다. 이 누락 복구를 모든 차이의 원인으로 설명해서는 안 된다.
- 8월 28일을 인위적으로 제외하는 대조 실험에서는 WMT 신호가 바뀌었으나, 기존 보고서의 다른 수치와 전부 일치하지 않았다. 실제 원인으로 판정하지 않는다. 운영 날짜/달력은 변경하지 않았다.
- NKE 39.06과 38.65의 차이는 원종가/배당 조정종가 차이로 확인됐다. 나머지 모든 주문 차이가 같은 원인인지는 입증되지 않았다.
- 최초 차이를 만든 9월 4일 실행 artifact에는 월별 후보와 원장은 있으나, 당시 전체 일별 OHLC 입력이 없다. 이후 입력으로 당시 값을 유일하게 복원할 수 없으므로 현재 확보한 증거로 모든 과거 차이의 원인을 확정할 수 없다.
- 운영 원장과 거래 전략은 변경하지 않았다. 기존 정기 결과 전체 또는 새 검증 전체를 무조건 정답이라고 판정하지 않는다.

검사 결과: `action-output/path-comparison/gap-signals.json`
화면 캡처: `artifacts/portfolio-clip-check/`
