# 날짜·이력 재발 방지 개선안

작성일: 2026-09-09 KST. 기준: `47d3f38` 및 그 이전 로컬 Git 이력.
요청 범위: 이전 수정까지 검토한 개선 방향 제시. 구현·배포는 수행하지 않았다.
이 문서는 기존 진단 보고서의 날짜·이력 개선안을 구체화한다.

## 1. 이전 수정에서 무엇이 해결됐고 무엇이 남았는가

커밋 메시지만 보지 않고 `git show`, `git blame`, 현재 코드와 테스트를 대조했다.

| 이전 변경 | 실제 변경과 현재 상태 | 남은 문제 및 이번 개선 |
| --- | --- | --- |
| `7dc07fb`, 8/16 | R2 index 복원, 본문·이미지 게시 후 index 게시. 현재 유지 | 기존 index가 이미 불완전하면 그대로 계승. 버전별 게시 manifest에서 목록 복구, 동시 갱신 충돌 차단 |
| `69e36ab`, 8/17 | 미국 거래일의 다음 평일 대신 요청한 한국 날짜를 표시일로 사용. 생성일보다 표시일이 미래인 기존 항목 제거. 현재 유지 | 날짜 의미 변경과 기존 데이터 삭제가 결합됨. 기존 기록은 보존·분류하고 새 게시만 검증 |
| `8667306`, 8/17 | 시장 보고서를 한국 월~금으로 제한. 포트폴리오 생성에 `--allow-stale-preview` 추가. 현재 유지 | 시장·매매테스트의 발행 정책이 다르고 preview 우회가 운영의 일반 경로가 됨. 공통 달력 계산과 보고서별 정책 분리 |
| `c270358`, 8/28 | IRCS 날짜별 보고서·영속 state·누락 거래일 순차 처리 도입 | 같은 거래일이면 빈 completedActions로 본문을 다시 쓰는 코드도 이때 도입되어 지금까지 유지. 거래일별 처리 결과를 불변 보관 |
| `e2ade85`, 8/28 | 미래일·19시 이전 발행 차단, reset_ledger 복구 옵션 추가. 현재 유지 | 발행 시각 검사는 중복 처리·원장 정합성을 검사하지 않음. 재실행·복구 모드를 명시적으로 분리 |
| `a72a2f6`, 8/29, 메시지 `market hoilday` | 정정본 조회 URL에 generatedAt 쿼리를 붙이는 프론트 캐시 보정. 현재 유지 | 휴장일 원장 처리 자체를 고친 커밋은 아님. 불변 revision 경로와 같은 거래일 no-op 필요 |
| `f10f6e5`, 8/30 | 전략 버전 변경 시 원장 재시작, 기존 다른 날짜 index 항목 유지. 현재 유지 | 저장 키에는 전략/원장 세대 구분이 없어 날짜가 겹치면 덮어쓰기 가능. ledgerId·strategyVersion으로 분리 |
| `9b6aa35`, 9/1 | 미완성 OHLC 제거, 전날 미국장 사용, 예약 19:13 이동, 19시 이전 지연 실행은 어제로 보정. 현재 유지 | 하루 이상 지연·미실행 날짜를 현재 시각으로 추정할 수 없음. 영속 작업 목록에서 대상일 결정 |
| `f88dbd9`, 9/4 | XNYS 휴장일 달력, 가격 신선도 검사, 캐시 대체·실패 진단·재시도 개선. 현재 유지 | 최신 캐시를 과거일로 자르는 상한과 전체 게시 정합성은 미해결. 기존 검사 유지하며 보강 |
| `47d3f38`, 9/4 | breadth에서 기준일과 다른 종목 제외. 현재 HEAD | 원장·목록·시각·재실행 문제는 이 변경의 대상이 아님 |

판단: 확인한 주요 방어 로직은 HEAD에서 사라지지 않았다. 특히 IRCS 빈 완료 내역
덮어쓰기는 이전 수정이 되돌아간 것이 아니라 최초 구현에서 남아 있던 경로다.
반면 운영 시장 index의 특정 항목이 실제로 언제 제외됐는지는 R2 변경 이력이 없어
커밋 이력만으로 확정하지 않는다. 운영 Worker 배포 SHA도 이번 조사에서는 확인하지 않았다.
이전 조사에서 확인한 9/9 실패 Actions SHA는 현재 HEAD와 일치했다.

## 2. 반드시 유지할 보장

절대 무장애를 보증할 수는 없다. 설계 목표는 날짜 경계·지연·휴장·재실행이 발생해도
**잘못된 날짜의 자료가 정상 게시되거나 기존 기록이 소실되지 않는 것**이다.
외부 수집 실패는 마지막 정상본 유지와 명시적 지연 상태로 처리한다.

1. 한국 보고일, 미국 거래일, 실제 생성 시각은 서로 다른 필드이며 서로 덮어쓰지 않는다.
2. 작업 시작 이후 대상일·자료 마감 시점은 고정된다. 실행이 자정을 넘어도 변하지 않는다.
3. 모든 입력 가격은 대상 거래일까지로 제한한다. 캐시 적중·대체·재실행에도 예외가 없다.
4. 동일 원장·전략·거래일은 계좌에 한 번만 반영된다. 재시도는 기존 결과를 그대로 읽는다.
5. 이미 공개한 본문·매매 이벤트는 덮어쓰지 않는다. 정정은 새 revision으로 남긴다.
6. 공개 목록·본문·계좌 state는 검증이 끝난 같은 게시 묶음을 참조한다.
7. 과거일 보충은 최신 포인터와 개인 저장 기록을 과거로 되돌리지 않는다.
8. 날짜 파싱 실패·자료 부족·정합성 오류는 게시 차단과 상태 기록으로 처리한다.

## 3. 날짜 계산을 한 곳으로 통합

`market_report_pipeline/report_time.py`와 `config/report-publication.yaml`을 새로 두는 안을 권고한다.
Python 공통 모듈은 대상일·거래일·예정 실행 시각을 계산하고, 모든 생성기가 같은
`ReportContext`를 입력받는다. YAML은 발행 요일·시간·시장·정책 버전을 관리한다.
프론트는 서버가 확정한 날짜와 공개 상태를 표시하며 공개 여부를 브라우저 시계로 재판정하지 않는다.

| 값 | 정의 |
| --- | --- |
| `reportDateKst` | 보고서별 발행 정책에 따라 귀속되는 한국 날짜. 실제 생성 날짜와 무관 |
| `marketSessionDate` | 자료에 사용한 실제 미국 거래일 |
| `valuationAt` | 혼합 한국·미국 자산을 평가한 UTC 시각 |
| `scheduledFor` | 해당 작업의 예정 시각, UTC ISO 8601 |
| `dataCutoffAt` | 이 보고서에 포함할 자료의 기준 마감 시각 |
| `generatedAt`, `publishedAt` | 실제 생성·게시 시각, UTC ISO 8601 |
| `nextExecutionSessionDate` | 전략에 따른 다음 실행 거래일 |
| `policyVersion`, `calendarVersion` | 발행 규칙·거래소 캘린더 버전 |

날짜는 유효성을 확인한 `YYYY-MM-DD`, 시각은 timezone-aware UTC로 저장한다.
거래소 계산은 `America/New_York`, 한국 표시일 계산은 `Asia/Seoul`을 사용한다.
미국-한국 시차를 고정 시간 덧셈으로 처리하지 않는다. 기존 timezone 없는 데이터는
이전 생성기의 확인된 규칙으로만 변환하며 출처가 불명확하면 `legacy-unverified`로 보존한다.

XNYS 달력 도입은 이미 되어 있으므로 다시 도입하는 작업이 아니다. 이를 시장·포트폴리오·IRCS
전체에 적용하고 실제 세션 종료 시각까지 사용하도록 확장한다. 다음 실행일을 미래 가격 행에서
찾는 `report_market_dates()`는 거래소 세션 조회로 대체한다. 신선도는 거래일과 실제 수집 상태로
판정하고, 미래 가격이 없다는 이유로 preview에 빠지는 우회는 제거한다.

### 보고서별 날짜 의미는 보존

시장·포트폴리오의 한국 평일 07:30 정책과 IRCS의 미국 거래일별 기록을 강제로 같은
날짜 목록으로 만들지 않는다. 초기 개선은 기존 정책을 유지하고 정의만 명문화한다.

예: 한국 9/8에는 시장 보고서가 한국 보고일 9/8·미국장 9/4를 표시한다.
IRCS는 기존 9/5 보고서를 보존하고, 별도 9/8 실행 상태에 '확인 완료·신규 거래일 없음'을 남긴다.
9/8 미국장이 닫힌 뒤 처리하는 9/9 IRCS에서 새 거래일 결과를 추가한다.
현재 아침/저녁 정책에서 쓰는 날짜 대응은 공통 달력 서비스가 담당한다.
발행 시간을 바꾸면 정책 버전을 올리고 관련 날짜 매핑 테스트도 함께 변경한다.

## 4. 예약 작업과 처리할 날짜를 분리

현재의 '19시 전이면 어제' 보정은 제거하고, 예약은 작업을 깨우는 신호로만 사용한다.
실행할 때마다 발행 정책에서 예정 시각이 지난 대상과 영속 처리 목록을 대조한다.
미처리·실패 대상을 오래된 순서로 처리하며, 각 대상의 context를 저장한 뒤 수집을 시작한다.
`lastSuccessDate` 하나만으로 판단하면 중간 구멍을 놓치므로 날짜별 처리 상태를 유지한다.

- 시장·포트폴리오: 한국 발행 슬롯별 `pending/running/published/failed` 관리.
- IRCS: 실제 미국 세션별 처리 원장과 한국 실행 시도 기록을 별도로 관리.
- 새 미국 세션이 없으면 원장을 그대로 두고 `no-new-session` 실행 상태만 추가.
- `workflow_dispatch --as-of`도 같은 planner·검증·게시 경로 사용.
- 호스트 `today()` 기본값과 여러 셸 단계에서의 날짜 재계산 제거.
- 별도 정기 점검은 예정일 누락을 찾아 재시도하고 지정 시간 초과 상태를 보고.

GitHub 예약 지연·누락 가능성은 공식 문서에 명시되어 있다. 따라서 cron 분을 바꾸는 기존
완화책은 유지할 수 있지만 정확한 날짜 처리의 전제로 삼지 않는다.
[GitHub 공식 문서](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)

## 5. 재실행·과거 복구·전략 초기화를 구분

처리의 고유 키는 공개 모델의 경우 `(reportType, policyVersion, reportDateKst)`,
IRCS 원장은 `(ledgerId, strategyVersion, marketSessionDate)`로 한다.
runId와 생성 시각은 실행 시도 식별자이며 거래 중복 방지 키로 쓰지 않는다.

| 모드 | 동작 |
| --- | --- |
| 정상 실행/재시도 | 이미 commit된 대상이면 저장된 결과 반환. 완료 매매와 최초 generatedAt 불변 |
| 누락일 보충 | 고정된 과거 대상일의 자료만 사용. 원장 세션을 순서대로 처리 |
| 보고서 정정 | 원본 보존 후 새 revision 생성. 최신 보고일보다 오래된 정정은 최신 날짜를 후퇴시키지 않음 |
| 원장 재구축 | 새로운 ledgerId에서 재생·대조. 검증 후 활성 원장 전환. 기존 원장·보고서 보존 |

IRCS 거래 이벤트에는 order/decision 식별자, 거래 세션, 실제 적용 가격·수수료·입력 해시를 저장한다.
이미 반영된 거래일이 최신 자료에서 잠시 사라지거나 더 오래된 캐시가 도착해도 state를 초기화하지 않는다.
`reset_ledger`와 전략 버전 변경은 운영 원장을 즉시 덮어쓰는 방식에서 분리해야 한다.

정확한 재현에는 가격 날짜 상한만으로 부족하다. 수정주가, 구성종목, 뉴스, 환율과 전략 버전의
당시 입력 스냅샷을 보관한다. 이후 받은 수정 데이터로 과거 결과를 계산하면 '재구성본'으로
표시한다. 당시 입력이 없으면 최초 발행본과 동일하다고 주장하거나 누락 기록을 추정 확정하지 않는다.

## 6. 기존 R2·Worker 구조에서 게시를 완결

당장 전체 서비스를 새 DB로 이전할 필요는 없다. R2의 불변 객체와 단일 활성 manifest를
사용하고, commit 경로를 인증된 Worker 경로 한 곳으로 모으는 방식을 권고한다.
Actions와 복구 도구가 활성 index/state를 직접 덮어쓰는 경로는 종료한다.

1. `releases/{releaseId}/`에 본문·이미지·입력 참조·index 버전·state 버전을 업로드한다.
2. 날짜·세션 상한·참조 존재·본문 해시·계좌 이벤트 합계를 검증한다.
3. 현재 manifest의 ETag가 작업 시작 때 읽은 값과 같을 때만 활성 참조를 갱신한다.
4. 다른 작업이 먼저 갱신했다면 최신 manifest를 읽고 병합·재계산·검증한다.
   이전 state에서 만든 IRCS 결과를 그대로 강제 게시하지 않는다.
5. API는 해당 manifest가 참조하는 객체만 공개한다. 중간 실패로 남은 미완성 객체는 공개하지 않는다.

아침 시장·포트폴리오는 함께 생성하는 한 release, 저녁 IRCS는 별도 release와 원장으로 관리한다.
별개 발행 정책끼리 불필요하게 성공을 기다리지 않는다. 여러 파일을 차례로 put하는 것을
트랜잭션이라고 간주하지 않고, 마지막 단일 참조 전환을 공개의 기준으로 삼는다.
Cloudflare Workers R2 API는 조건부 쓰기 `onlyIf`를 제공한다. Python S3 클라이언트와
동일한 옵션이라고 가정하지 않고 Worker commit 경로에서 사용·검증한다.
[Cloudflare 공식 문서](https://developers.cloudflare.com/r2/api/workers/workers-api-reference/)

본문/이미지는 revision 또는 hash 경로에서만 immutable로 제공한다.
기존 날짜 API는 활성 revision을 찾는 호환 API로 유지하되 가변 응답에 장기 immutable을 적용하지 않는다.
목록·최신·운영 상태의 초기 캐시 목표는 최대 60초로 하고 실제 CDN·브라우저 설정까지 대조한다.
다른 release가 섞이면 프론트는 같은 release를 재조회한다.

## 7. 목록·개인 기록·과거 데이터 이관

- index는 원본이 아니라 commit된 manifest에서 재생성할 수 있는 조회 자료로 만든다.
- 기존 520개 제한은 화면 페이지 크기/페이지 분할로 바꾸고 보존 기록을 잘라내지 않는다.
- `generatedAt < displayDate`인 과거 항목을 매일 제거하는 규칙은 종료한다.
  불확실한 과거 항목은 별도 상태와 원본 경로를 남긴다. 미래 공개 차단은 신규 commit 검증이 담당한다.
- 이관에서는 구형 객체를 목록화하고 본문 메타데이터·전략 버전·실제 생성기 규칙으로 대응표를 만든다.
  파일명에 일괄 +1/-1을 적용하지 않는다. 중복 날짜는 덮어쓰기 대신 구형 revision으로 보관한다.
- 신규 구조에서는 미완성 staging 파일을 단순 열거해서 index에 복구하지 않는다.
  정상 commit 이력을 기준으로 복구하고, 구형 이관 대상만 별도 검증한다.
- 브라우저 저장에서 오래된 priceDate가 새 snapshot을 지우는 동작을 제거한다.
  기록은 안정된 ID로 병합하고 자료 revision 및 실제 recordedAt을 따로 저장한다.
- 현재 수량으로 재계산한 추이는 시뮬레이션, 당시 기록은 실제 이력으로 구분한다.
  한국·미국 혼합 평가는 valuationAt에 이미 이용 가능한 시장별 가격·환율로 계산한다.
- 공개 포트폴리오에는 날짜별 archive를 추가한다. 개인 기기간 동기화는 별도 기능이며,
  시간대 통일만으로 브라우저 밖에 데이터가 보존되는 것은 아니다.

이관 전 백업·해시 목록을 확보한다. 신규 읽기 경로 검증 후 구형 writer를 중단하고 활성
manifest를 전환한다. 롤백은 이전 검증 release를 읽게 하는 방식이며 새로운 원장 이벤트를
삭제하거나 구형 writer가 다시 덮어쓰도록 하지 않는다.

## 8. 테스트를 재발 상황 중심으로 교체·추가

기존 테스트에서 날짜 함수·index 중복 제거·업로드 순서는 다뤘지만 다음 보장은 부족했다.
특히 `test_update_index_removes_report_generated_before_its_display_date`는 항목 삭제를
정답으로 고정한다. 신규 공개 차단과 기존 기록 보존을 각각 검사하도록 교체한다.
IRCS의 `test_report_index_is_dated_and_idempotent`는 목록 개수만 확인하므로
실제 `run()`과 저장 계층을 두 번 실행해 계좌·완료 매매 불변성을 확인해야 한다.

| 시나리오 | 통과 조건 |
| --- | --- |
| 9/4 미국장 처리 후 9/7 휴장을 거친 재실행 | 완료 매매·청산 누계·수수료·본문 해시 불변, 실행 확인 시각만 갱신 |
| 같은 날짜 2회 및 동시 2회 실행 | 이벤트 중복 0, commit 충돌은 재검증, 실제 계좌 반영 1회 |
| 19:13 작업이 자정·다음날 19시 이후·48시간 뒤 실행 | 예정된 미처리 슬롯을 빠짐없이 원래 날짜로 처리 |
| UTC/KST/New York 환경, DST 전환, 조기폐장, 연말·윤일 | context와 세션 매핑이 동일하고 미완료 가격 포함 0 |
| 최신 캐시로 과거일 생성, 다운로드 실패 대체, provider 미래 행 | 모든 수집 경로에 target session 상한 적용 |
| 업로드·검증·manifest 전환 각 지점에서 강제 실패 | 마지막 정상 release 공개 유지, 재시도 후 완전한 새 release |
| 새 보고서 뒤 과거일 정정·과거 API 응답 도착 | 최신 날짜와 새 개인 snapshot 보존 |
| 날짜가 겹치는 전략 변경·원장 초기화 | 구형 원장·원본 보고서 유지, 새 ledger로 분리 |
| 목록 누락·손상, 구형 날짜 규칙 혼합 | 검증된 원본에서 복구, 임의 삭제·미완성 게시 0 |
| 화면 장시간 열기·다른 브라우저 시간대·캐시된 정정 전 본문 | 서버 날짜 유지, 같은 release 조회, 최신 상태 재확인 |

공통 context fixtures를 Python·TypeScript·Worker 계약 테스트에 공유한다.
기존 회귀 테스트와 새 통합 테스트를 변경 PR의 필수 체크로 만들고,
배포 후 실제 API의 schema/policy/code SHA·release·거래일을 확인해 배포 누락도 잡는다.
이 계획 단계에서는 테스트를 새로 실행하지 않았으며, 위 항목은 구현 완료 판정 기준이다.

## 9. 권고 적용 순서

1. 기존 자료 백업, 현재 오류 재현 fixture 확보, IRCS 동일 거래일 덮어쓰기와 과거 캐시 혼입 차단.
2. 공통 날짜 계약·planner와 반복/지연 실행 테스트 구현. 기존 휴장일·신선도 검사 유지.
3. 불변 보고서·원장·조건부 manifest commit, 과거 목록 복구·정정 방식 구현.
4. API·프론트 캐시/표시·개인 snapshot 단조성 개선, 구형 데이터 이관 검증.
5. 기존 발행과 별도 경로에서 결과를 대조한 뒤 전환. 누락 대상·지연·원장 불일치 자동 감시.

완료 기준은 정상일 1회 실행 성공이 아니다. 위 재발 상황을 모두 통과하고,
운영에서도 기록 변경 없이 재실행·휴장·지연 복구가 확인되어야 완료로 판정한다.
