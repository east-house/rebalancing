# 보고서 기록·시간대·GitHub Actions 진단

후속 검토: 이전 수정 커밋과 테스트까지 추적한
[날짜·이력 재발 방지 개선안](DATE_RELIABILITY_PLAN_2026-09-09.md)을 추가했다.
날짜 관련 최종 개선 방향은 후속 문서를 우선 참조한다.

조사일: 2026-09-09 KST. 대상 커밋: `47d3f385d170104f4260a5ee49fafd7565e444ae`.
범위: 코드, GitHub 공개 실행/단계/진단 메타데이터, 운영 공개 API의 읽기 전용 조사.
애플리케이션 수정, 배포, R2 변경, 워크플로 재실행은 수행하지 않았다.

## 1. 결론

문제는 단일 UTC/KST 변환 오류가 아니다. 실제 기록 덮어쓰기, 목록 누락,
저장 자체가 없는 이력, 거래일과 발행일의 서로 다른 정의, 예약 지연,
실행 실패 및 캐시·대체 데이터가 함께 작용한다.

우선순위는 **기존 데이터 보존 → 덮어쓰기 차단 → 목록 복구 → 실패 진단 및 재게시
→ 날짜 계약 통일 → 이력 저장·갱신 구조 개선**이다.

사용자가 언급한 `https://tm-report.com`은 조사 환경에서 HTTP 403이었다.
저장소 README에 명시된 `https://tm-reports.com`에서는 실제 서비스 API를 확인했다.
아래 운영 관측은 복수형 도메인 기준이며, 두 도메인의 연결 관계는 확인하지 못했다.

## 2. 실제 운영에서 확인한 내용

| 대상 | 관측 | 판단 |
| --- | --- | --- |
| 시장 보고서 목록 | 9/8, 9/7 두 건만 존재 | 과거 목록 누락 확인 |
| 시장 보고서 9/4 본문 | 날짜별 API에서 정상 반환, 미국장 9/3 | 본문 삭제와 목록 누락은 별개. 적어도 이 날짜는 본문 재수집 없이 목록 복구 가능 |
| 시장 보고서 최신 | 한국 보고일 9/8, 미국장 9/4 | 9/7 미국 휴장에 따른 정상 기준일 |
| 포트폴리오 최신 | 한국 보고일 9/8, 미국장 9/4, `stale_preview=true` | 예비 데이터 상태가 운영 화면에 충분히 드러나지 않음 |
| 매매테스트 최신 | 보고일 9/5, 미국장 9/4, 재생성 시각 9/8 23:31 KST | 미국 휴장으로 새 거래일이 없는 것은 정상. 기존 거래일 본문 재작성은 별도 문제 |
| 매매테스트 9/5 | G55 `closedTrades=1`, `completedActions=[]` | 계좌 청산 누계와 해당 거래일 상세 기록이 불일치 |
| 직전 매매테스트 9/4 | G55 TFC 매도 예정, `closedTrades=0` | 다음 보고서의 청산 증가·TFC 보유 제거와 맞물리지만 완료 매도 내역이 없음 |

시장 목록은 별도 쿼리 문자열을 사용한 요청에서도 동일했다.
R2 객체 전체 목록이나 저장소 버전 이력은 조회하지 않았으므로 과거 전체 누락 범위는 미확정이다.

운영 근거:

- [시장 목록](https://tm-reports.com/api/market-reports)
- [목록에서 누락된 9/4 시장 본문](https://tm-reports.com/api/market-reports/2026-09-04)
- [포트폴리오 최신](https://tm-reports.com/api/portfolio-reports/latest)
- [매매테스트 목록](https://tm-reports.com/api/trading-test-reports)
- [9/4 매매테스트](https://tm-reports.com/api/trading-test-reports/2026-09-04)
- [9/5 매매테스트](https://tm-reports.com/api/trading-test-reports/2026-09-05)

## 3. 실패한 GitHub Actions

[실행 34295117348](https://github.com/east-house/rebalancing/actions/runs/34295117348)
의 `Generate and publish daily US market report`가 실패했다.
실행 SHA는 조사한 로컬 코드와 일치한다.

| 항목 | 확인 결과 |
| --- | --- |
| 이벤트 | `schedule`, 실행은 실제 생성됨 |
| 실행 생성 | 2026-09-09 09:27:52 KST |
| 작업 시작 | 09:27:54 KST |
| 실패 단계 | `Collect data and generate report` |
| 실패 단계 시간 | 09:28:29 ~ 09:28:59 KST |
| 종료 | 09:29:03 KST, exit code 1 |
| 선행 단계 | 의존성 설치, 파이프라인 테스트, 한국 날짜 계산, R2 설정 검사 성공 |
| 후속 단계 | 포트폴리오 게시와 시장 웹 묶음 게시 모두 skipped |

따라서 이번 건은 예약이 전혀 실행되지 않은 문제가 아니라, 수집·생성 프로그램의
실패 때문에 당일 두 보고서 게시가 중단된 사건이다. R2 설정 검사의 성공은 실제
R2 쓰기 성공을 입증하지는 않지만, 이번 실패는 게시 단계보다 앞에서 발생했다.

설정된 07:30과 실행 생성 시각의 차이는 약 1시간 58분이다. 실행 생성 후 runner
시작은 약 2초 후이므로 관측된 지연의 대부분은 생성 이전에 있다. 정확한 내부
스케줄러 지연 원인은 GitHub 공개 메타데이터만으로 확정할 수 없다.
현재 GitHub는 `schedule.timezone`을 지원하며, 부하에 따른 예약 지연·누락 가능성도
문서화한다. `timezone: Asia/Seoul`을 무효 문법으로 진단하거나 UTC로 중복 보정하면 안 된다.
[GitHub 공식 문서](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule)

### 직접 예외 확인 (2026-09-10 정정)

인증된 로그와 artifact를 확보하여 `fresh=6, required=490` 가격 신선도 검증 실패를 확인했습니다.
547종목에 기대 거래일 2026-09-08의 완성 가격이 없었습니다.
아래는 최초 조사 당시의 제약과 가설이며, 구성목록 타임아웃 가설은 이번 실패 원인에서 제외합니다.
상세 근거는 [실패 원인 확인 보고서](./ACTIONS_FAILURE_RCA_2026-09-10.md)를 참고하세요.

### 최초 조사 당시 상태

공개 annotation에는 `Process completed with exit code 1.`만 있다.
상세 로그 API는 HTTP 403, 진단 artifact 다운로드는 HTTP 401을 반환했다.
따라서 특정 제공자 장애나 시간대 오류를 이번 실행의 직접 원인으로 확정할 수 없다.

약 30초 만의 실패와 `support.py:69`의 신규 날짜별 S&P 500 목록 요청
(`requests.get`, `timeout=30`, 재시도·직전 스냅샷 대체 없음)은 조사할 후보다.
그러나 가격 신선도 검증 등 다른 예외도 가능하므로 실행 시간만으로 결론내리지 않는다.

진단 artifact `market-report-34295117348`는 조사 당시 보존 중이며,
만료 예정은 **2026-09-16 09:29 KST**다. 아래 파일의 예외·제공자·신선도를 확인해야 한다.

- `failure_diagnostics.json`: 예외 종류, 메시지, traceback.
- `pipeline.log`: 어느 수집 단계까지 진행됐는지.
- `price_collection_audit.json`: 가격 수집 단계에 도달했다면 존재할 수 있으며, 기대 거래일·신선한 종목 수·실패 종목 확인.

수정 방향: 일시적 네트워크 오류에 한정된 재시도, 허용 기한을 정한 구성종목 스냅샷 대체,
거래일별 신선도 검증 유지, 구조화된 실패 원인/대상일을 Actions Summary와 운영 상태 파일에 기록.
오류 확인 후 `as_of=2026-09-09`로 명시 재실행하며, 아래 과거자료 상한 필터부터 수정해야 한다.

## 4. 코드상 문제와 수정안

### P1. 같은 거래일 재실행이 완료 매매 기록을 지운다

근거: `market_report_pipeline/us_ircs_forward_report.py:844`, `:891`.
새 거래일이 없고 `latest == last`이면 `completed`를 빈 배열로 만들어 기존 날짜의
보고서를 생성한다. 이후 `reports/{reportDate}.json`과 `latest.json`을 덮어쓴다.
계좌 누계는 유지되면서 해당 날짜 완료 매매만 사라지는 구조이며 운영 관측과 일치한다.

수정: 이미 처리한 거래일은 기존 보고서를 그대로 반환하는 no-op으로 처리한다.
매매 이벤트를 별도 영속 원장에 저장하고 보고서는 원장에서 구성한다.
정정이 필요하면 기존 본문을 보존한 revision을 생성한다. 같은 날짜 재실행,
휴장일 재실행, 상태 저장 직전 실패 후 재시도에도 거래 내역·수량·수수료가 동일해야 한다.
현재 idempotent 테스트는 목록 중복만 검사하므로 실제 `run()` 재실행 테스트가 필요하다.

### P1. 본문과 날짜 목록이 분리되어 과거 보고서가 숨는다

근거: `publish_market_report_web.py:155`, `:237`. 기존 index만 읽고 병합하며
R2에 남은 날짜별 본문에서 목록을 재구성하는 기능이 없다. `generatedAt`에서 계산한
날짜가 `displayDate`보다 이르면 기존 index 항목을 제외하는 정리 로직도 존재한다.
현재 9/4 본문은 정상이고 목록에서 빠진 사실은 확정이지만, 이 항목이 언제 어떤
작업으로 제외됐는지는 기존 R2 index 이력·실행 로그가 있어야 확정 가능하다.

수정: R2 날짜별 본문을 열거하고 본문 메타데이터를 검증해 index 복구안을 만든다.
불일치 항목은 삭제 대신 격리·진단한다. index와 원장 변경 전 버전 백업,
객체 해시와 revision, 원자적으로 전환하는 release manifest를 도입한다.

### P1. 최신 캐시로 과거일을 재실행하면 미래 가격이 섞일 수 있다

근거: `us_market_report.py:199`, `:224`, `:234`, `:1745`.
캐시 적중·다운로드 실패 대체 경로는 시작일만 제한하고 요청 거래일까지의 상한을
제한하지 않는다. 다운로드 병합 경로도 완료 거래일 대신 한국 보고일까지 허용한다.
이후 benchmark 전체 최댓값으로 시장일을 정한다.

예: 9/8 미국 종가까지 들어 있는 캐시로 한국 9/7 보고서를 재생성하면,
원래 사용해야 할 9/4 이후 가격이 포함될 수 있다. 운영 과거 보고서의 실제 오염
여부는 별도 검증이 필요하지만 코드 경로상 위험은 명확하다.

수정: 모든 반환 경로에서 `history_start <= date <= expected_market_date`를 적용한다.
뉴스·구성종목 자료도 당시 사용 가능 시점을 구분한다. 과거 시점 원자료가 없으면
현재 자료로 재계산한 결과를 당시 발행 원본으로 표시하지 않는다.

### P1. 포트폴리오 보고서 과거본과 개인 이력이 자동 누적되지 않는다

근거: `us_daily_portfolio_report.py:510`은 `portfolio-reports/latest.json`만 업로드.
`PortfolioReportPage.tsx:138`의 DAILY 기록은 브라우저에서 페이지가 열릴 때 추가되고
최근 180건만 남는다. 같은 보고일이면 정정본이 와도 기존 DAILY 기록은 갱신하지 않는다.

수정: 공개 모델 보고서에 날짜별 본문·index·revision을 추가한다. 개인 이력은 별도
문제로 취급한다. 먼저 백업 내보내기/가져오기와 저장 실패 표시를 제공하고,
기기간 동기화까지 필요하면 인증·사용자별 영속 저장을 설계한다.
현재 기록에는 전체 당시 포지션·거래 스냅샷이 없으므로 미방문일의 실제 투자 이력을
정확하게 자동 복원할 수 있다고 약속하면 안 된다.

### P2. 실행 예정일을 과거 가격에서 찾고 예비 상태를 숨긴다

근거: `us_daily_portfolio_report.py:133`, `:174`, `:377`, `:459`.
`calendar`는 과거 IVV 가격 인덱스인데 여기서 한국 보고일 이후 거래일을 찾는다.
정상 아침 수집에는 미래 미국 종가가 없으므로 이 조건은 충족되기 어렵다.
운영은 `--allow-stale-preview`로 이를 우회해 실행 예정일을 보고일로 지정한다.
미국 휴장일에는 이 날짜가 실제 거래일이 아닐 수도 있다.

또한 payload에 실제 생성 시각이 없고 화면은 `07:30 KST`를 고정 출력한다.
`stale_preview`를 화면에서 구분하지 않아 예비 상태와 실제 최신 상태를 판별하기 어렵다.

수정: 완료 거래일·다음 주문 가능 거래일은 XNYS 거래소 캘린더와 실행 시각으로 계산.
실제 생성 시각·공개 시각·예정 시각을 각각 저장하고, 데이터 신선도와 실행일
추정 상태를 분리한다. 신선한 종가에 미래 날짜가 없다는 이유로 stale로 분류하지 않는다.

### P2. 보고일의 정의와 예정 시간·실제 시간이 섞인다

근거: 시장 보고서는 manifest `as_of`인 한국 평일을 사용하고,
매매테스트 `us_ircs_forward_report.py:729`는 미국 거래일에 달력 하루를 더한다.
시장 작업은 월~금 07:30, 매매테스트는 화~토 19:13으로 정책도 다르다.
CLI의 `pd.Timestamp.today()`는 호스트 시간대에 의존한다.
시장 날짜 필터는 시간대 없는 과거 `generatedAt`을 브라우저 로컬 시간으로 해석한다.
현재 운영 시장 timestamp에는 `+09:00`이 있어 이 마지막 위험을 직접 확인한 것은 아니다.

수정: 날짜 전용 값은 문자열로 유지하고 시각만 timezone-aware UTC로 저장한다.
거래소 달력은 `America/New_York`, 한국 표시일은 `Asia/Seoul`로 명시한다.
지연된 실행의 대상일은 실행 도중의 today에서 다시 추정하지 않고 처음에 고정한다.
실패일은 영속 실행 목록에서 찾아 보충한다. 모든 보고서를 무조건 하루 이동하지 않는다.

### P2. 재게시 후에도 오래된 자료가 정상처럼 보일 수 있다

근거: `worker/index.js:210`은 R2 오류·객체 부재를 모두 정적 파일로 대체한다.
시장 index는 공용 helper의 `s-maxage=3600`, `stale-while-revalidate=86400` 정책을
받는다. 정적 시장 index는 빈 목록이고 정적 매매테스트는 8/27 스냅샷이다.
매매테스트 날짜별 API는 immutable이지만 서버는 같은 경로 본문을 재작성한다.
프론트의 generatedAt 쿼리 보정은 일부 경로만 보호하며 직접 URL에는 부족하다.
`MarketReportPage.tsx:168`은 현재 시각과 최초 목록을 계속 유지하고 주기 갱신이 없다.

수정: 운영 장애와 정적 데모 대체를 구분하고 `source`, `isStale`, `lastSuccessAt`,
`expectedMarketDate`를 전달한다. 가변 index/latest는 짧게 캐시하고,
본문·이미지는 revision/hash URL에서만 immutable로 제공한다.
화면 복귀·예정 발행 시점 이후 목록을 다시 조회하고 release ID 일치를 검증한다.

### P2. 자산 추이 차트는 실제 과거 계좌 기록이 아니다

근거: `src/domain/history.ts:18`, `src/App.tsx:683`.
현재 보유 수량과 현금을 과거 가격 전체에 적용한 재계산 결과가 저장 snapshot보다
우선 표시된다. 수량을 바꾸면 과거 차트도 달라진다. 이는 문서화된 현재 기능이지만
실제 투자 기록으로 기대하면 오류처럼 보인다.
한국·미국 종가도 날짜 문자열로 결합하므로 같은 실제 관측 시점의 계좌 평가와 다르다.
`portfolioStorage.ts:108`은 더 오래된 priceDate 입력 시 그 이후 snapshot도 제거한다.

수정: 실제 평가 이력과 현재 보유 기준 시뮬레이션을 구분하고 실제 이력을 기본으로 제공.
평가 시점에 이미 확정된 시장별 가격·환율과 당시 보유수량을 사용한다.
오래된 API 응답으로 새 snapshot이 삭제되지 않게 저장의 단조성을 보장한다.

## 5. 통일할 날짜·저장 계약

| 필드 | 의미 |
| --- | --- |
| `reportDateKst` | 한국 기준으로 이 보고서를 귀속시킬 업무 날짜 |
| `marketSessionDate` | 사용한 실제 미국 거래일, 날짜 전용 |
| `scheduledFor` | 예정 실행 시각, UTC ISO 8601 |
| `generatedAt` | 실제 생성 시각, UTC ISO 8601 |
| `publishedAt` | 게시 완료 시각, UTC ISO 8601 |
| `expectedMarketSessionDate` | 캘린더상 확보됐어야 하는 완료 거래일 |
| `proposedExecutionSessionDate` | 전략상 다음 주문 실행 거래일 |
| `runId`, `releaseId`, `revision`, `sourceHash` | 실행·묶음·정정·원자료 추적 |
| `status`, `dataStatus` | 실행 성공/실패/휴장/지연과 자료 신선도를 각각 표현 |

9/7 미국장은 Labor Day로 휴장했다. 따라서 한국 9/8 보고서가 미국 9/4 종가를
사용하고 거래일 기반 매매테스트의 최신 보고일이 9/5로 남는 것은 정상이다.
이때 운영 상태는 '9/8 확인 완료, 신규 거래일 없음'으로 갱신하고 기존 9/5 본문은 보존해야 한다.
[NYSE 공식 캘린더](https://www.nyse.com/publicdocs/nyse/ICE_NYSE_2026_Yearly_Trading_Calendar.pdf)

## 6. 적용 순서와 완료 기준

1. R2의 index·날짜별 보고서·IRCS state를 별도 백업하고 객체 목록·해시를 보관한다.
   9/9 실패 artifact도 만료 전에 확보한다.
2. IRCS 기존 거래일 덮어쓰기와 과거 재실행의 미래 가격 혼입을 먼저 차단한다.
3. 남아 있는 본문으로 시장 index를 재구성한다. 9/5 TFC 완료 매도 상세는 원장,
   당시 artifact, 원자료로 검증해 복구하고 기존 손상본도 revision으로 남긴다.
   복구할 수 없는 기록은 결측으로 표시한다.
4. 9/9 직접 예외를 확인해 원인별 수정 후 날짜를 명시해 재생성·게시한다.
   두 보고서 검증이 모두 끝난 release만 최신 포인터로 전환한다.
5. 날짜 계약, 거래소 캘린더, 공개 보고서 archive, 운영 갱신 상태, 캐시 정책을 적용한다.
6. 개인 기록의 백업·영속화 범위와 실제 이력/시뮬레이션 표시를 개선한다.

필수 검증:

- 같은 미국 거래일 재실행·휴장일 실행에도 완료 매매·수수료·계좌 해시 불변.
- R2 게시 각 단계에서 실패시킨 뒤 재시도해도 본문/index/state가 같은 release를 가리킴.
- 최신 캐시를 사용한 과거일 재실행에도 해당 시점 이후 가격이 0건.
- KST 자정, 미국 DST 전환, 토요일 아침, 9/7 휴장, 지연·수동 실행의 날짜 매핑.
- 목록의 모든 항목에 본문이 있고, 보존 대상 본문은 모두 index에 포함됨.
- R2 오류 때 정상 최신 자료로 오인하지 않으며 오래된 응답이 새 개인 기록을 지우지 않음.
- 재게시·화면 장시간 열기·브라우저 시간대 변경 후에도 표시일과 release가 일치.

이번 조사에서는 공개 API 자료와 코드 경로를 대조했다. Python pandas/pytest 및
프론트 의존성이 설치되지 않은 환경에서 애플리케이션 테스트는 새로 실행하지 않았다.
9/9 Actions의 기존 파이프라인 테스트 성공과 실제 수집 단계 실패는 공개 단계 결과로 확인했다.
